@import 'variables/colors';

.login {
  height: 50vh;
  background-color: primary;
}

.app {
  height: 300px;
  background-image: linear-gradient(darkPrimary, darkPrimary 20%, primary 20%, primary);
}